import { Typography } from "@mui/material";

function App() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100%',
    }}>
      <Typography variant="h3" component="h2">
        Hello World!!
      </Typography>  
    </div>
  );
}

export default App;
